function menuPopup() {
    var menuChange = document.getElementById("myNavigation");
    if (menuChange === "Navigation") {
        menuChange += " responsive";
    } else {
        menuChange = "Navigation";
    }
}